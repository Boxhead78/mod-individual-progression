/*
 * The .ip command suite is intentionally disabled in this fork.
 * Keep the registration symbol because IndividualProgression_loader.cpp
 * calls it unconditionally.
 */

void AddSC_individualProgression_commandscript()
{
}
